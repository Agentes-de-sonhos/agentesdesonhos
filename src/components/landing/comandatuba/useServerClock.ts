// Kept for backwards compatibility: the drift-safe clock now lives in
// src/hooks/useServerClock.ts so every public surface shares one implementation.
export { useServerClock } from "@/hooks/useServerClock";
